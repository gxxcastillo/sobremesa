{
  "name": "database",
  "tags": ["scope:infra"],
  "targets": {
    "types": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p libs/database/tsconfig.lib.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint libs/database"
      }
    },
    "check-db": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsx scripts/check-db.ts"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "libs/database/vite.config.ts"
      }
    }
  }
}
