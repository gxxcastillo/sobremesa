{
  "name": "prompts",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/prompts/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: bun nx show project prompts --web",
  "targets": {
    "types": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun tsc --noEmit -p libs/queue/tsconfig.lib.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun eslint libs/queue"
      }
    }
  }
}
