{
  "name": "agents-facilitator",
  "tags": ["scope:service"],
  "targets": {
    "types": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p libs/agents/facilitator/tsconfig.lib.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint libs/agents/facilitator"
      }
    }
  }
}
