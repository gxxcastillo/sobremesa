{
  "name": "agents-filter",
  "tags": ["scope:async"],
  "targets": {
    "types": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p libs/agents/filter/tsconfig.lib.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint libs/agents/filter"
      }
    }
  }
}
