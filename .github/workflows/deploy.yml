name: Deploy

on:
  workflow_run:
    workflows: ['CI']
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      run_ci:
        description: 'Run CI checks'
        type: boolean
        default: true

permissions:
  actions: read
  contents: read

jobs:
  ci:
    if: github.event_name == 'workflow_dispatch' && inputs.run_ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.8

      - run: bun install --frozen-lockfile

      - uses: nrwl/nx-set-shas@v4

      - name: Run CI
        run: npx nx affected -t lint test build types

  check-affected:
    needs: [ci]
    if: |
      always() &&
      (needs.ci.result == 'success' || needs.ci.result == 'skipped') &&
      (github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success')
    runs-on: ubuntu-latest
    outputs:
      api: ${{ steps.affected.outputs.api }}
      chatbots: ${{ steps.affected.outputs.chatbots }}
      db: ${{ steps.affected.outputs.db }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.8

      - run: bun install --frozen-lockfile

      - uses: nrwl/nx-set-shas@v4
        id: nx-shas

      - name: Determine affected projects
        id: affected
        run: |
          # Fallback: if no base SHA, deploy all
          if [ -z "$NX_BASE" ] || [ "$NX_BASE" = "$NX_HEAD" ]; then
            echo "No base SHA - deploying all"
            {
              echo "api=true"
              echo "chatbots=true"
              echo "db=true"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi

          AFFECTED=$(npx nx show projects --affected --json 2>/dev/null | jq -r '.[]' 2>/dev/null || echo "")

          API=$(echo "$AFFECTED" | grep -q "^api$" && echo "true" || echo "false")
          CHATBOTS=$(echo "$AFFECTED" | grep -q "^chatbots$" && echo "true" || echo "false")
          DB=$(echo "$AFFECTED" | grep -q "^db$" && echo "true" || echo "false")

          {
            echo "api=$API"
            echo "chatbots=$CHATBOTS"
            echo "db=$DB"
          } >> "$GITHUB_OUTPUT"

          echo "Affected: api=$API chatbots=$CHATBOTS db=$DB"

  deploy-api:
    needs: check-affected
    if: fromJSON(needs.check-affected.outputs.api)
    uses: ./.github/workflows/deploy-api.yml
    with:
      run_ci: false
    secrets: inherit

  deploy-chatbots:
    needs: check-affected
    if: fromJSON(needs.check-affected.outputs.chatbots)
    uses: ./.github/workflows/deploy-chatbots.yml
    with:
      run_ci: false
    secrets: inherit

  deploy-db:
    needs: check-affected
    if: fromJSON(needs.check-affected.outputs.db)
    uses: ./.github/workflows/deploy-supabase.yml
    with:
      run_ci: false
    secrets: inherit
